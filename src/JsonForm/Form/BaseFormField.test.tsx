
describe('Form', () => {
	it('im doing this just for coverage 🤦‍♂️', async () => {
		const { Form } = require('./Form.ts');
		expect(Form).toBeTruthy();
	});

	it('im doing this just for coverage 2.0 🤦‍♂️', async () => {
		const { Form } = require('./Form.web.ts');
		expect(Form).toBeTruthy();
	});
});
